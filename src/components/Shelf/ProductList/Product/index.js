import React from 'react';

const Product = ({ product }) => {
    console.log(product);
    return (
        <div className="shelf-item">
          PRODUCT
        </div>
      );
};

export default Product;